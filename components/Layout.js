const Layout = ({ children }) => {
  return (
    <div className="relative font-poppins cursor-default min-h-screen w-full bg-purple-50">
      {children}
    </div>
  )
}

export default Layout
